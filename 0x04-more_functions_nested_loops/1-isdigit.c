/**
 * _isdigit - checking digits from 1-9
 * @c: character this is a.
 * Return: 1 if c is a digit, if not then return 0.
 */
int _isdigit(int c)
{
if (c >= '0' && c <= '9')
return (1);
else
return (0);
}
